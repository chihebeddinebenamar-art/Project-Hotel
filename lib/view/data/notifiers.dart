import 'package:flutter/foundation.dart';

/// Index de l’onglet bas — même principe que `ProjectFlutter` (`selectedIndexNotifier`).
final ValueNotifier<int> mainTabIndexNotifier = ValueNotifier<int>(0);
